# Travel App

## Initial Setup
To run this project, install it locally using npm:

Server Side
```
$ cd travel_app_node
$ npm install
$ node server.js
```

Client Side
```
$ cd travel_app_react
$ npm install
$ npm start
```
